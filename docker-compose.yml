services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: prowider
      POSTGRES_PASSWORD: prowiderpassword
      POSTGRES_DB: prowider_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
